# Климат Лайн — сайт по продаже и установке кондиционеров

> Живой документ. Правим и дополняем по ходу работы.
> Последнее обновление: 2026-09-03

---

## 1. Контекст

**Заказчик:** частный мастер (работает один), дилер компании [swcomfortair.by](https://swcomfortair.by/).
**Регион:** Минск и Минская область.
**Что делаем:** имиджевый сайт-визитка с каталогом-витриной.
**Что НЕ делаем:** интернет-магазин. Нет корзины, нет онлайн-оплаты, нет личного кабинета.
Конверсия = заявка (форма / звонок / мессенджер), а не заказ.

**Ключевой посыл сайта (из макета):** «Работаю один — отвечаю за результат лично.
Без посредников и лишних наценок.»

---

## 2. Стек и ключевые решения

| Решение | Выбор | Почему |
|---|---|---|
| Сборка | **Vite 8 + React 19 + TypeScript** | Привычный стек, быстрый dev. |
| Роутинг | **React Router 7** (`react-router-dom`) | Обычный декларативный роутинг, без файловой магии. |
| Пререндер | **`vite-react-ssg`** | На билде раскладывает все роуты в готовые HTML-файлы. Чистый SPA брать нельзя: Яндекс рендерит JS медленнее и менее надёжно, чем Google, а сайт живёт с локального поиска. |
| Стилизация | **SCSS + CSS Modules** (`*.module.scss`) | Стили скоупятся автоматически, имена классов не конфликтуют. Vite поддерживает из коробки — нужен только пакет `sass-embedded`. |
| Данные | **Supabase** (Postgres + Storage + Auth) | Товары, отзывы, портфолио, заявки. Даёт готовую авторизацию и хранилище картинок для админки (этап 6). |
| Отдача каталога | Фетч из Supabase **на этапе билда** → пререндер | Клиент получает статику, поисковики — готовый HTML. Supabase дёргается при сборке, а не у посетителя. |
| Формы | Serverless-функция хостинга → **Telegram Bot API** | Токен бота нельзя держать в клиенте, нужен хоть какой-то сервер. |
| Хостинг | Vercel / Netlify / Cloudflare Pages | Статика + одна функция. Уточнить у заказчика — см. §11. |
| Анимации | Framer Motion (точечно) | Только появление секций и hover. Без «вау». |
| Иконки | lucide-react | Тонкие контурные — совпадают с макетом. |

### Почему не Next.js
Обсудили и отказались. Единственное, что он давал сверх Vite + пререндера — sitemap,
метатеги и эндпоинт формы «из коробки». Это пишется руками за пару часов и не стоит
церемоний с RSC и `"use client"` на каждом интерактивном фильтре.
`next/image` тоже не аргумент: картинки товаров мы всё равно жмём в WebP скриптом
импорта через `sharp` (§5).

> **Риск:** `vite-react-ssg` — версия 0.x, небольшой мейнтейнер. Если подведёт,
> запасной вариант — свой пререндер-скрипт на Playwright по списку роутов
> (собрать HTML после гидрации и разложить по папкам). Проверяем библиотеку
> на этапе 0, пока проект пустой.

### Версии на старте (проверено 2026-09-03)
`vite@8.2.2` · `react-router-dom@7.18.3` · `sass-embedded@1.103.1` · `vite-react-ssg@0.9.2`

---

## 3. Дизайн-система (снята с макета)

### Цвета
```css
--brand-600: #3B5BFE;   /* основной синий: кнопки, ссылки, акценты */
--brand-700: #2F49D8;   /* hover */
--brand-50:  #EEF2FF;   /* подложки блоков, иконки */
--ink-900:   #0F172A;   /* заголовки */
--ink-600:   #475569;   /* основной текст */
--ink-400:   #94A3B8;   /* подписи, мелочь */
--surface:   #FFFFFF;   /* карточки */
--bg:        #F8FAFF;   /* фон страницы, чуть голубоватый */
--border:    #E6EAF5;
```

### Типографика
- Шрифт: **Manrope** или **Inter** (кириллица, переменный). Ставим локально через
  `@fontsource-variable/manrope` — без запроса к Google Fonts, чтобы не терять LCP.
- H1 — 48/56, 700; H2 — 32/40, 700; H3 — 20/28, 600; body — 16/26, 400; caption — 14/22.

### Компоненты (общие правила)
- Радиусы: карточки `16px`, кнопки `10px`, бейджи `999px`.
- Тени: очень мягкие — `0 1px 2px rgba(15,23,42,.04), 0 8px 24px rgba(15,23,42,.04)`.
- Карточка: белая, `1px` бордер `--border`, без тени в покое, лёгкая тень на hover.
- Иконки: контурные, синие, в квадрате `40x40` с фоном `--brand-50`.
- Синий баннер-секция (как «Работаю один») — сплошной `--brand-600`, белый текст, радиус `20px`.
- Контейнер: `max-width: 1200px`, боковые отступы `24px` (моб. `16px`).
- Вертикальный ритм секций: `96px` desktop / `56px` mobile.

### Организация стилей

```
src/styles/
├─ index.scss          # точка входа: reset + токены + базовая типографика
├─ _tokens.scss        # :root { --brand-600: ... } — CSS custom properties
├─ _breakpoints.scss   # $breakpoints (SCSS map) + миксин mq()
├─ _mixins.scss        # container, text-preset, focus-ring, card
└─ _reset.scss
```
Рядом с компонентами — `Button.module.scss`, `Hero.module.scss` и т.д.

**Токены — CSS custom properties, а не SCSS-переменные.** SCSS-переменные исчезают
при компиляции; кастомные свойства живут в рантайме — видны в девтулзах,
переопределяются в медиазапросе или на отдельной секции. Для дизайн-системы это важнее,
чем компайл-тайм подстановка.

**SCSS берём ради того, чего нет в CSS:** миксины (медиазапросы, типографские пресеты),
вложенность, `@use` вместо простыни импортов.

```scss
// _breakpoints.scss
$breakpoints: (sm: 480px, md: 768px, lg: 1024px, xl: 1280px);

@mixin mq($key) {
  @media (min-width: map.get($breakpoints, $key)) { @content; }
}

// использование
.card {
  padding: 16px;
  @include mq(md) { padding: 24px; }
}
```

> **Ловушка, о которую легко споткнуться:** `_tokens.scss` содержит `:root { ... }`,
> поэтому подключается **один раз** в `index.scss`. Если написать `@use "tokens"`
> в каждом `.module.scss`, блок `:root` продублируется в бандле столько раз,
> сколько компонентов. Через `@use` подключаем только `_mixins` и `_breakpoints` —
> они компайл-тайм и в бандл сами по себе ничего не добавляют.

---

## 4. Карта сайта

```
/                          Главная
/quiz                      Подбор кондиционера — квиз отдельной страницей (для рекламы)

/catalog                   Каталог — все 44 модели
/catalog/[category]        4 категории: split · multi-split · mobile · semi-industrial
/product/[slug]            Карточка модели
/solutions                 Готовые решения — список
/solutions/[slug]          Решение под площадь (4-5 посадочных)

/services                  Услуги — список
/services/[slug]           Продажа / Установка / Обслуживание / Ремонт
/price                     Прайс на монтаж — под запрос «сколько стоит установка»

/articles                  Полезное — список
/articles/[slug]           Статья (3-5 на старте)

/about                     Обо мне
/contacts                  Контакты (карта, реквизиты, мессенджеры)
/privacy                   Политика обработки перс. данных (обязательна по закону РБ)
/404                       Страница не найдена

/portfolio                 ⚑ выключено флагом — нет фото работ
/reviews                   ⚑ выключено флагом — нет отзывов
/admin                     Админка (этап 6). Вне пререндера, sitemap и robots.txt
```

### Что изменилось и почему

| Правка | Причина |
|---|---|
| Добавлен индекс `/solutions` | Были детальные страницы без списка: дыра в хлебных крошках и в меню, перелинковку не построить |
| Каталог разведён: `/catalog/[category]` и `/product/[slug]` | `/catalog/[slug]` был занят товаром, категорию туда не вставить. Заодно получаем 4 посадочные под «мобильный кондиционер Минск» |
| Добавлен `/price` | «Сколько стоит установка кондиционера» — частотный коммерческий запрос, вести по нему было некуда. **Зависит от цены монтажа** (§11, заглушка) |
| Добавлены `/articles` | Свои тексты — основной источник органики, раз карточки товаров дублируют поставщика (§9). 3-5 статей на старте |
| `/portfolio` и `/reviews` спрятаны за флаг | Контента нет. Пустая страница в индексе хуже отсутствующей. Разворачиваем обратно, когда появятся фото и отзывы |
| Добавлен `/404` | Просто забыли |

### Слаги — свои, не поставщика
У поставщика слаг товара это процентно-кодированная кириллица:
`%d0%ba%d0%be%d0%bd%d0%b4%d0%b8%d1%86%d0%b8%d0%be%d0%bd%d0%b5%d1%80-ultima...`
Нечитаемо, разваливается при копировании, плохо выглядит в выдаче.
Скрипт импорта генерирует **свой транслитерированный слаг** из бренда и модели:
`gree-pular-r410-09` — и кладёт его в поле `slug`, а исходный URL остаётся в `source_url`.
Слаг фиксируется при первом импорте и **больше не меняется**: смена URL опубликованной
страницы = потеря позиций.

### Ещё две идеи — после запуска, не в первый релиз

**Гео-посадочные.** Для локального бизнеса это главный SEO-резерв: «установка
кондиционера Заславль / Фаниполь / Дзержинск / Боровляны». Конкуренция там ниже,
чем по Минску. **Но:** 30 страниц по шаблону с подменой города — это дорвеи,
за них прилетает фильтр. Делать 2-3 штуки с реально разным содержанием
(сроки выезда, доплата за расстояние, примеры работ в этом районе) или не делать вовсе.

**Страница под сезонное обслуживание.** «Чистка кондиционера Минск цена» —
низкоконкурентный запрос с повторяющимся спросом каждую весну. Сейчас это
подраздел `/services/[slug]`; если пойдёт трафик — вынести в отдельную посадочную.

### Блоки главной — 7 секций

Сознательно сжато с 12. Половина блоков в первой версии повторяла друг друга:
преимущества дважды, товары трижды, доверие двумя секциями подряд.
Правило: **один посыл — один блок.**

1. **Hero** — надзаголовок «Комфорт в вашем доме и офисе», H1 «Продажа и установка
   кондиционеров в Минске и области», подзаголовок, 2 кнопки («Подобрать кондиционер» →
   скролл к квизу, «Получить консультацию» → модалка), фото сплит-системы справа.
   Снизу — **полоса из 4 преимуществ** с иконками (Один специалист / Честные цены /
   Качественный монтаж / Поддержка и сервис).
2. **Квиз «Подберу за 4 вопроса»** — главный конверсионный блок, ставим высоко на странице (см. §5).
3. **Готовые решения по площади** — 4–5 карточек с ценой «от».
4. **Услуги** — 4 карточки, превью раздела.
5. **«Работаю один — отвечаю за результат лично»** — синий баннер (из макета страницы услуг)
   + под ним **«Как я работаю»**, 5 шагов со стрелками. Вместе отвечают на «почему он» и
   «как это будет» — по отдельности эти блоки слабее.
6. **Работы и отзывы** — сетка фото «до/после» + 2–3 отзыва, ссылки на полные страницы.
7. **FAQ + форма заявки** — аккордеон на 5–6 вопросов, сразу под ним форма.
   Порядок важен: FAQ снимает последние возражения, форма ловит готового клиента.

**Что убрано и почему:**
- «Почему выбирают меня» (4 карточки) — дублировало полосу преимуществ под hero.
- CTA-баннер «Нужна помощь с выбором?» — его работу делает квиз, он занял это место.
- «Популярные модели» — модели уже показаны в решениях и в результатах квиза.
- Отзывы и портфолио как отдельные секции — слиты в один блок доверия.

### Шапка / подвал
- **Меню — 5 пунктов, не больше:** Каталог · Решения · Услуги · Обо мне · Контакты
  + кнопка «Связаться» (синяя, pill). В макете было 6 с Портфолио и Отзывами —
  они выключены до появления контента, а Каталог важнее обоих.
- Квиз в меню не выносим: он второй секцией на главной, кнопкой в hero и ссылками
  из решений. Отдельный `/quiz` нужен для рекламных ссылок, а не для навигации.
- Шапка sticky, при скролле — тень.
- Мобильное меню: полноэкранное, кнопка «Позвонить» внизу.
- **Подвал — полная карта сайта:** сюда уходит то, чему не хватило места в меню
  (Цены, Полезное, Политика), плюс контакты, мессенджеры, ИП/УНП.

---

## 5. Каталог: источник данных

**Проверено:** на swcomfortair.by открыт публичный WooCommerce Store API — парсер HTML не нужен.

```
GET https://swcomfortair.by/wp-json/wc/store/v1/products?per_page=100&page=N
GET https://swcomfortair.by/wp-json/wc/store/v1/products/categories
```
- Всего товаров: **4284**.
- Заголовки ответа: `X-WP-Total`, `X-WP-TotalPages` — по ним гоняем пагинацию.
- Цена приходит в минорных единицах: `prices.price = "210000"`, `currency_minor_unit = 2` → **2100.00 BYN**.
- Характеристики лежат в `attributes[]` — их **~55 штук** на товар, реально полезных ~25.

### Категории (id | slug | количество)
```
33     split-system              Сплит-системы                     3410
28512  полупромышленная-...      Полупромышленная сплит-система     244
14646  multi-split-sistema       Мульти-сплит система               231
34     mobile                    Мобильные системы                  148
32410  vrf-vrv-sistemy           VRF/VRV системы                    141
28927  бытовая-сплит-система     Бытовая сплит-система               26
32505  precision                 Прецизионное                        22
32521  holodilnoe                Холодильное оборудование            18
35     multi-split               Мульти сплит-системы                14
29942  system-chiller            Чиллер-фанкойл                      12
37     window                    Оконные системы                      5
16     other                     Другие                               1
```

> ### Тянем НЕ всё. Решение: 4-5 готовых решений + 30-60 моделей
>
> Полную витрину на ~800 позиций сознательно **не делаем**. Причина не в трудозатратах
> (скрипт импорта одинаковый), а в трёх вещах:
>
> 1. **SEO.** Характеристики копируются у поставщика — тот же текст стоит на
>    swcomfortair.by и у десятка других дилеров. 800 страниц дублированных
>    спецификаций на молодом домене не ранжируются, это thin content.
>    10-15 своих текстов дадут больше трафика.
> 2. **Цены протухнут.** 40 позиций мастер проверит за вечер, 800 — никогда.
> 3. **Доверие.** Витрина-агрегатор противоречит позиционированию
>    «работаю один, отвечаю лично», и растёт риск запроса модели, которую он не возит.

### Структура вместо каталога-витрины

**Ядро — «Готовые решения по площади».** Продаём решение, а не SKU; клиент думает
комнатами, а не киловаттами. 4-5 блоков на главной + отдельная посадочная на каждый:

| Решение | Площадь | Цена |
|---|---|---|
| Спальня | до 20 м² | от 1 400 р. под ключ |
| Гостиная | 25-35 м² | от 1 900 р. |
| Квартира-студия | 40-50 м² | от 2 400 р. |
| Офис | 50-70 м² | от 3 100 р. |

*Цифры условные — уточнить у заказчика (§11).*
В каждом решении — 3 рекомендованные модели: **бюджет / оптимум / премиум** — и кнопка заявки.

**Под этим — курируемый каталог на 30-60 моделей.** Только бренды, которые мастер
реально ставит. Выгружаем через API один раз, дальше правим руками в JSON.

**Широту ассортимента отдаём текстом, а не страницами** — блок на главной и в каталоге:

> «Работаю со всем каталогом поставщика — 4000+ моделей.
> Нужной нет на сайте? Позвоните, привезу.»

### Отбор моделей — квоты и критерии (решение за нами)

Заказчик отдал выбор на наше усмотрение. Берём по квоте на категорию:

| Категория | ID | Берём |
|---|---|---|
| Сплит-системы (основа каталога) | 33 | **20** |
| Мульти-сплит системы | 14646 | **8** |
| Мобильные кондиционеры | 34 | **8** |
| Полупромышленные сплит-системы | 28512 | **8** |
| **Итого** | | **44** |

**Коммерческие категории не берём** — VRF (141), чиллеры (12), прецизионное (22),
холодильное (18), оконные (5). Это оборудование под бригаду и проект, а не под
одного мастера; на сайте оно только размывает позиционирование.
Если понадобятся — квота 8 на категорию, +32 позиции, правка одной константы в скрипте.

- **Бренды:** ограничиваем 6–8 узнаваемыми (Gree, Haier, TCL, Electrolux, Ballu,
  Royal Clima, Dantex, Mitsubishi). Меньше брендов — выше доверие к подборке.
- **Обязательная сетка:** 5 классов площади (20 / 25 / 35 / 50 / 70 м²) × 3 tier
  (бюджет / оптимум / премиум) = 15 позиций-«скелет». Она гарантирует, что квиз
  не упрётся в пустую выдачу ни на одной комбинации.
- В каждом классе площади — **и инверторная модель, и on/off**: без инвертора
  не закрыть ответ «хочу греться зимой».
- **Приоритет `is_in_stock === true`.** «Под заказ» берём, только если иначе
  дыра в сетке.
- Внутри квоты добираем популярными моделями среднего ценового сегмента.

> **Осторожно с `short_description` поставщика.** Он состоит из двух частей: факты
> («сплит-система, мощность 7 кВт, площадь 70 м²») и **их маркетинговый хвост**
> («Купите с установкой ПОД КЛЮЧ! Рассрочка 0% до года, Лизинг и Кредит, работаем с НДС»).
> Хвост копировать нельзя — это чужие условия продажи, мы рассрочку не даём.
> Скрипт импорта режет всё после перечисления характеристик.

### Цены

**Цена оборудования — как у поставщика,** наценка `1.0`. Заказчик зарабатывает на монтаже.
Коэффициент всё равно выносим в `config/pricing.ts` — если решит добавить процент,
это правка одного числа, а не переобход каталога.

**Цены монтажа в API нет** — проверено, `prices` отдаёт только оборудование,
«с установкой» на сайте поставщика считается их фронтом. Поэтому:

```ts
// config/pricing.ts
export const MARKUP = 1.0;              // наценка на оборудование
export const INSTALL_PRICE = {          // TODO: заглушка, уточнить у заказчика
  upTo25m2: 450,
  upTo50m2: 600,
  over50m2: 800,
};
```
- Пока цифры не подтверждены — на карточке показываем **цену оборудования**
  и отдельной строкой «+ монтаж от N р.». Как только заказчик даст реальный прайс,
  переключаем на слитое «от X р. под ключ» — это флаг в конфиге, не переписывание вёрстки.
- Цены готовых решений (§5, таблица) считаются автоматически: самая дешёвая
  подходящая модель + монтаж, округление вверх до 50 р. Руками их не ведём.

### Полезные атрибуты (маппинг в наши поля)
| Атрибут поставщика | Наше поле |
|---|---|
| Бренд | `brand` |
| Модель | `model` |
| Тип кондиционера | `type` |
| Обслуживаемая площадь | `areaM2` (число: «70м2» → 70) |
| Мощность охлаждения / обогрева | `coolingKw` / `heatingKw` |
| Инверторная технология | `isInverter` (есть/нет → bool) |
| Хладагент (фреон) | `refrigerant` |
| Wi-Fi | `hasWifi` |
| Шум внутреннего блока | `noiseDb` |
| Рабочая температура при обогреве | `minHeatTemp` |
| Энергоэффективность (EER / COP) | `eer` / `cop` |
| Габариты и вес блоков | `dimensions.*` |

### Скрипт импорта `scripts/import-catalog.ts`
Запускается **вручную и редко**, не в CI. Пишет напрямую в Supabase
(`service_role`-ключ, только локально — в браузер он не попадает никогда).

- [ ] Идёт по квотам из таблицы выше, применяет критерии отбора.
- [ ] Нормализует атрибуты в типизированный объект (парсинг чисел из строк «7кВт», «70м2», «34 — 42дБ»).
- [ ] Режет маркетинговый хвост `short_description` (см. выше).
- [ ] Считает `price` = цена поставщика × `MARKUP`.
- [ ] Скачивает изображения, конвертит в WebP (`sharp`), ресайзит 800/400,
      заливает в **Supabase Storage** (бакет `products`, публичное чтение).
      Хотлинк на чужой домен не используем — имена файлов кириллические и могут отвалиться.
- [ ] **`upsert` по `source_id`, не `insert`.** Обновляет только поля поставщика
      (`price`, `specs`, `in_stock`, `images`) и **не трогает** поля, которые ведутся
      руками или через админку: `description`, `tier`, `featured`, `sort_order`,
      `is_published`. Иначе первый же рефреш цен снесёт всю ручную работу.
- [ ] Режим `--refresh-prices` — обновить только цены и наличие.
- [ ] Лог: что изменилось в цене, что пропало из наличия у поставщика.
- [ ] `--dry-run` — показать diff без записи.

### Фильтры в каталоге
Только два: **площадь помещения** и **цена**. Плюс сортировка по цене.
Слайдеров по уровню шума, EER и типу фреона не делаем — на 40 позициях они бессмысленны.
Состояние — в URL (`?area=25`), чтобы ссылки шарились.

### Тексты карточек
Спецификации оставляем как есть (это факты), но **описание в 2-3 предложения пишем своё** —
почему мастер эту модель рекомендует и кому. Это единственное, что отличает наши карточки
от карточек поставщика в глазах поисковика. На 40 позициях реально написать; на 800 — нет.

---

## 5.1 Квиз «Подберу кондиционер за 4 вопроса»

Главный конверсионный инструмент. Блок на главной (секция 2) + отдельная страница `/quiz`
для рекламных ссылок. Кнопка «Подобрать кондиционер» в hero скроллит к нему.

### Вопросы
| № | Вопрос | Варианты | Что определяет |
|---|---|---|---|
| 1 | Какая площадь помещения? | до 20 · 20–35 · 35–50 · 50–70 · больше 70 м² | мощность, решение |
| 2 | Что охлаждаем? | квартира · частный дом · офис · другое | тип блока, нюансы монтажа |
| 3 | Нужно ли греться зимой? | да, вместо обогревателя · нет, только охлаждение · не знаю | инвертор / тепловой насос |
| 4 | Ориентир по бюджету? | эконом · оптимальный · премиум · посоветуйте сами | `tier` модели |

### UX — что делает его «красивым и понятным»
- **Один вопрос на экран**, крупные карточки-варианты с иконкой. Клик по варианту
  **сразу листает дальше** — без кнопки «Далее», так вдвое быстрее.
- Прогресс «Шаг 2 из 4» + тонкая полоса заполнения. Кнопка «Назад» всегда доступна.
- Переход между шагами — fade + сдвиг 200 мс. Высота контейнера анимируется, чтобы
  страница не прыгала. Больше никаких анимаций.
- **Результат показываем сразу, без ввода телефона.** Прятать выдачу за контактами —
  главная ошибка таких квизов: убивает доверие и половину конверсии. Телефон просим
  только на шаге «заказать консультацию».
- Вариант «не знаю / посоветуйте» есть в каждом сложном вопросе — не загоняем в тупик.
- Мобильный: одна колонка, тач-таргеты от 56 px.
- Доступность: `role="radiogroup"`, выбор стрелками и цифрами 1–4, фокус переносится
  на первый вариант нового шага, прогресс озвучивается через `aria-live`.

### Экран результата
- Заголовок по-человечески: «Для комнаты 25 м² с обогревом зимой подойдут эти три».
- Строка **«Почему эти»** — одно предложение с объяснением (инвертор, запас по мощности).
- **3 карточки моделей**: бюджет / оптимум / премиум, с ценой «от X р. под ключ».
- Две кнопки:
  - **«Заказать консультацию»** — первичная, синяя. Открывает форму с уже
    подставленными ответами (клиенту ничего не переписывать).
  - **«Посмотреть весь каталог»** — вторичная. Ведёт в `/catalog` с применённым
    фильтром площади: `/catalog?area=25`.
- Ссылка «Пройти заново» — мелко, под кнопками.

### Логика подбора
```
1. Фильтр: specs.areaM2 >= требуемая площадь (с запасом ~15%)
2. Если «греться зимой» → только isInverter === true
3. Из оставшихся берём по одной модели каждого tier, сортируя по priceFrom
4. Если под комбинацию ничего не нашлось (на 40 позициях это будет) →
   экран «Подберу индивидуально» + форма. Пустую выдачу не показываем никогда.
```

### Технические детали
- Ответы кладём в URL: `/quiz?area=20-35&place=flat&heat=yes&budget=mid`.
  Даёт кнопку «Назад» в браузере, шаринг ссылки и отдельный URL результата для аналитики.
- Ответы уходят в заявку (`Lead.quizAnswers`) — мастер видит контекст до звонка.
- Цели аналитики: старт квиза, каждый шаг (где отваливаются), показ результата,
  клик по консультации. Отваливание на конкретном шаге — сигнал переписать вопрос.
- Логика подбора — чистая функция в `lib/quiz.ts`, покрыть юнит-тестами,
  включая случай пустой выдачи.

---

## 5.2 Хранилище: Supabase

### Поток данных
```
swcomfortair API  ──(scripts/import-catalog.ts, вручную)──>  Supabase
                                                                 │
                          админка (этап 6) ─── правки ──────────>│
                                                                 │
                                              (фетч на этапе билда)
                                                                 ▼
                                                    пререндер → статика → CDN
```
Посетитель в Supabase не ходит: он получает готовый HTML. База нужна на сборке
и в админке. Это сохраняет и скорость, и SEO.

### Таблицы
| Таблица | Назначение | Ключевые поля |
|---|---|---|
| `products` | Каталог, 44 позиции | `source_id` (uniq, id у поставщика), `slug`, `name`, `brand`, `type`, `price`, `images[]`, `in_stock`, `specs` (jsonb), `description`, `tier`, `featured`, `sort_order`, `is_published` |
| `solutions` | Готовые решения по площади | `slug`, `title`, `area_from`, `area_to`, `intro`, `product_ids[]` |
| `reviews` | Отзывы | `author`, `text`, `rating`, `date`, `is_published`, `sort_order` |
| `portfolio` | Работы «до/после» | `title`, `description`, `images[]`, `date`, `is_published` |
| `leads` | Заявки (история сверх телеграма) | `name`, `phone`, `message`, `source`, `product_id`, `quiz_answers` (jsonb), `status`, `created_at` |

**Storage:** бакет `products` (картинки товаров, публичное чтение),
бакет `portfolio` (фото работ, публичное чтение, загрузка из админки).

**Разделение полей `products` — важное:**
- *Поля поставщика* (`price`, `specs`, `in_stock`, `images`) — перезаписываются импортом.
- *Наши поля* (`description`, `tier`, `featured`, `sort_order`, `is_published`) —
  импорт их не трогает, ведутся руками и через админку.

### RLS — включить сразу, на всех таблицах
- `products`, `solutions`, `reviews`, `portfolio`: **anon читает только `is_published = true`**,
  пишет — только `authenticated`.
- `leads`: **anon не может ни читать, ни писать.** Запись — только через
  serverless-функцию с `service_role`. Там персональные данные, читать их из браузера нельзя.

### Ключи
| Ключ | Где живёт | Правило |
|---|---|---|
| `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` | клиентский бандл | Так и задумано — но **только при включённом RLS**. Без RLS anon-ключ = открытая база. |
| `SUPABASE_SERVICE_ROLE_KEY` | скрипт импорта + `api/lead.ts` | **Никогда** с префиксом `VITE_`. Обходит RLS полностью. |

### Пересборка после правок
Правка в админке не видна на сайте, пока не пересобрали статику. Решение:
Database Webhook в Supabase → build hook хостинга. В админке — кнопка
«Опубликовать изменения» с индикатором, чтобы заказчик понимал, что происходит.
Каталог меняется редко, ребилд раз в правку — нормально.

### Типы
`supabase gen types typescript` → `src/types/database.ts`, генерируем при изменении схемы.
Руками типы таблиц не пишем — разъедутся.

### Если Supabase недоступен на сборке
Билд **падает с ошибкой**, а не выкатывает пустой каталог. Для локальной разработки
держим снапшот `src/data/seed.json` — он же годится, чтобы поднять базу с нуля.

---

## 6. Структура проекта

```
conditioner-app/
├─ PLAN.md
├─ vite.config.ts
├─ index.html
├─ api/
│  └─ lead.ts            # serverless-функция: приём заявок → Telegram
├─ src/
│  ├─ main.tsx           # входная точка vite-react-ssg
│  ├─ routes.tsx         # объявление роутов + getStaticPaths для /catalog/[slug]
│  ├─ styles/            # index.scss, _tokens, _mixins, _breakpoints, _reset
│  ├─ pages/             # Home, Quiz, Catalog, CatalogCategory, Product,
│  │                     # Solutions, SolutionDetail, Services, ServiceDetail, Price,
│  │                     # Articles, ArticleDetail, About, Contacts, Privacy, NotFound,
│  │                     # Portfolio + Reviews (за флагом), Admin (этап 6)
│  ├─ components/
│  │  ├─ ui/             # Button, Card, Badge, Input, Modal, Accordion, Section
│  │  ├─ layout/         # RootLayout, Header, Footer, MobileMenu, Container, Breadcrumbs
│  │  ├─ sections/       # Hero, Advantages, WhyMe, Steps, CtaBanner, Faq, Reviews
│  │  ├─ catalog/        # ProductCard, ProductGrid, Filters, Specs
│  │  ├─ quiz/           # Quiz, QuizStep, QuizOption, QuizProgress, QuizResult
│  │  ├─ seo/            # Seo.tsx — метатеги и JSON-LD (react-helmet-async)
│  │  └─ forms/          # LeadForm, CallbackModal
│  ├─ data/              # seed.json (снапшот для локальной разработки), services.ts, faq.ts
│  ├─ lib/               # supabase.ts (клиент), queries.ts, format.ts, seo.ts,
│  │                     # filters.ts, quiz.ts (логика подбора + тесты)
│  ├─ types/             # database.ts (сгенерирован), product.ts, lead.ts
│  └─ config/            # site.ts (контакты, соцсети, FEATURES), pricing.ts
├─ supabase/
│  ├─ migrations/        # схема таблиц и RLS-политики в git
│  └─ seed.sql           # наполнение чистой базы
├─ scripts/
│  ├─ import-catalog.ts  # выгрузка товаров с swcomfortair.by → Supabase
│  └─ gen-sitemap.ts     # генерация sitemap.xml после билда
└─ public/               # catalog/, portfolio/, icons/, robots.txt
```

**Что делаем руками вместо Next:**
- Метатеги и JSON-LD — `react-helmet-async` (`vite-react-ssg` вшивает их в статический HTML).
- `sitemap.xml` — скрипт `gen-sitemap.ts` в `postbuild`, обходит список роутов + слаги товаров.
- OG-картинки — одна статическая на сайт + по одной на раздел, рисуем заранее.
- Оптимизация изображений — в скрипте импорта (`sharp` → WebP, 800/400) и вручную для контента.
- Ленивая загрузка картинок — обычный `loading="lazy"` + явные `width`/`height`.

---

## 7. Модели данных

> Ниже — форма данных в приложении (camelCase). Типы таблиц **генерируем** из Supabase
> в `types/database.ts`, руками их не пишем. `lib/queries.ts` маппит snake_case из БД
> в эти типы — один слой маппинга, чтобы компоненты не знали про схему БД.

```ts
type Product = {
  id: number;
  slug: string;
  name: string;
  brand: string;
  model: string;
  type: 'split' | 'multi-split' | 'mobile' | 'semi-industrial';
  priceFrom: number;               // BYN, «под ключ»: оборудование + наценка + монтаж
  images: string[];                // локальные пути
  inStock: boolean;

  // --- поля, которые ведём вручную; скрипт импорта их не трогает ---
  description: string;             // 2-3 предложения своими словами: кому и почему
  tier: 'budget' | 'optimum' | 'premium';
  featured: boolean;               // показывать в блоке «Популярные модели»
  solutionSlugs: string[];         // в каких готовых решениях участвует

  specs: {
    areaM2?: number;
    coolingKw?: number;
    heatingKw?: number;
    isInverter?: boolean;
    hasWifi?: boolean;
    refrigerant?: string;
    noiseDb?: number;
    eer?: number;
    cop?: number;
    minHeatTemp?: number;
    [key: string]: unknown;        // остальное — в таблицу «Все характеристики»
  };
  sourceUrl: string;               // ссылка на карточку поставщика (для сверки)
  updatedAt: string;
};

type Solution = {
  slug: string;                    // bedroom-20, living-room-35, studio-50, office-70
  title: string;                   // «Кондиционер для спальни до 20 м²»
  areaFrom: number;
  areaTo: number;
  priceFrom: number;               // «от 1 400 р. под ключ»
  intro: string;                   // свой текст для SEO-посадочной
  productSlugs: string[];          // 3 модели: бюджет / оптимум / премиум
};

type Lead = {
  name: string;
  phone: string;
  message?: string;
  source: 'hero' | 'quiz' | 'product' | 'footer' | 'modal';
  productSlug?: string;
  quizAnswers?: Record<string, string>;
};
```

---

## 8. Формы и заявки

- Валидация: `react-hook-form` + `zod`. Телефон — маска `+375 (XX) XXX-XX-XX`.
- Чекбокс согласия на обработку персональных данных со ссылкой на `/privacy` — **обязателен** (Закон РБ №99-З).
- Антиспам: honeypot-поле + rate-limit по IP в route handler. Капчу ставим только если пойдёт спам.
- Отправка: `POST /api/lead` — serverless-функция хостинга (`api/lead.ts` в корне: формат
  подходит и Vercel, и Netlify Functions). Функция делает две вещи:
  **пишет заявку в таблицу `leads`** (история и будущая админка) и **шлёт в Telegram**
  (мгновенное уведомление). Если телеграм упал — заявка всё равно сохранена, клиент не потерян.
  Токен и чат — только в переменных окружения хостинга (`TELEGRAM_BOT_TOKEN`,
  `TELEGRAM_CHAT_ID`), локально в `.env.local`. **В клиентский бандл они попасть не должны** —
  никаких `VITE_` префиксов у этих переменных.
- В dev — тот же эндпоинт через `vite dev` middleware или `vercel dev`.
- Дублирование на email — по желанию (Resend).
- После отправки — экран «Спасибо» + событие в аналитике.

---

## 9. SEO, производительность, аналитика

- [ ] **Проверить, что пререндер реально работает:** `curl` по собранным страницам должен
      отдавать готовый текст и метатеги, а не пустой `<div id="root">`. Это ключевая проверка стека.
- [ ] Уникальные `title` / `description` на каждой странице через `<Seo />`, шаблон в `lib/seo.ts`.
- [ ] Schema.org: `LocalBusiness` + `HVACBusiness` (адрес, телефон, часы, гео), `Product` на карточках, `FAQPage`, `BreadcrumbList`.
- [ ] **Основная ставка — не карточки товаров, а свои тексты:** страницы решений,
      услуг и 3-5 статей. Карточки поставщика есть у всех дилеров, тексты — только у нас.
- [ ] `sitemap.xml` (скрипт в `postbuild`), `robots.txt`, канонические URL.
- [ ] OG-картинки: статические, по одной на раздел.
- [ ] Все изображения — WebP, явные `width`/`height`, `loading="lazy"`. Hero — `fetchpriority="high"` и без lazy.
- [ ] Цель Lighthouse: **90+** по всем четырём метрикам на мобильном.
- [ ] Яндекс.Метрика (вебвизор + цели) и/или GA4.
- [ ] Ключевые цели: отправка формы, клик по телефону, клик по мессенджеру, прохождение квиза.
- [ ] Подключить Яндекс.Вебмастер + Google Search Console, добавить в Яндекс.Бизнес.
- [ ] Доступность: контраст AA, фокус-стили, alt у всех картинок, семантика.

---

## 10. Этапы работ

### Этап 0. Подготовка
- [ ] Скаффолд Vite + React + TypeScript, `sass-embedded`, ESLint/Prettier + stylelint.
- [ ] Подключить `vite-react-ssg` + React Router, **сразу проверить пререндер** на двух
      тестовых страницах (см. §9). Если не заводится — переходим на запасной вариант из §2.
- [ ] Дизайн-токены в `styles/_tokens.scss`, миксины, подключение шрифта.
- [ ] `config/site.ts` — контакты, соцсети, тексты-заглушки, `FEATURES`.
- [ ] Базовые UI-компоненты (Button, Card, Section, Container, Badge, Input).
- [ ] **Supabase:** завести проект, миграции со схемой (§5.2), **включить RLS на всех
      таблицах сразу** — не «потом, перед запуском». Проверить анонимным ключом,
      что `leads` не читается, а неопубликованные товары не видны.
- [ ] `lib/supabase.ts` + генерация `types/database.ts`.

### Этап 1. Каркас и главная (7 секций, см. §4)
- [ ] Header + MobileMenu + Footer.
- [ ] Hero + полоса преимуществ.
- [ ] Секции «Услуги», синий баннер + «Как я работаю», «Работы и отзывы».
- [ ] FAQ + форма заявки.
- [ ] Адаптив 360 / 768 / 1024 / 1440.
- [ ] Место под квиз (секция 2) — заглушкой, реализация в этапе 3.

### Этап 2. Внутренние страницы
- [ ] Услуги: список + 4 детальные, хлебные крошки.
- [ ] Обо мне, Контакты (карта Яндекс), Политика, 404.
- [ ] `/price` — прайс на монтаж (после того как заказчик даст цифры).
- [ ] `/articles` + 3-5 статей.
- [ ] Портфолио и Отзывы — сверстать, оставить под флагом до появления контента.

### Этап 3. Решения и каталог
- [ ] Блок «Готовые решения» на главной + 4-5 посадочных `/solutions/[slug]` со своими текстами.
- [ ] Скрипт импорта, выгрузка 44 моделей по квотам в Supabase, ручная чистка.
- [ ] `lib/queries.ts` + фетч на этапе билда, `getStaticPaths` по слагам из базы.
- [ ] Каталог: сетка, два фильтра (площадь, цена), сортировка. Пагинация не нужна.
- [ ] Карточка товара: галерея, «от X р. под ключ», своё описание, ключевые характеристики,
      полная таблица, CTA, 2-3 похожие модели.
- [ ] Блок «работаю со всем каталогом поставщика, спрашивайте».

### Этап 3.1. Квиз (см. §5.1)
- [ ] `lib/quiz.ts` — логика подбора + юнит-тесты, **обязательно случай пустой выдачи**.
- [ ] 4 шага, синхронизация состояния с URL, кнопка «Назад».
- [ ] Экран результата: 3 модели по tier, «Почему эти», две CTA-кнопки.
- [ ] Фолбэк «Подберу индивидуально», если под комбинацию ничего не нашлось.
- [ ] Проброс ответов в форму заявки (`Lead.quizAnswers`).
- [ ] Клавиатура и скринридер: `radiogroup`, стрелки, `aria-live` на прогрессе.
- [ ] Отдельная страница `/quiz` с тем же компонентом.

### Этап 4. Заявки и интеграции
- [ ] `/api/lead` + Telegram, модалка обратного звонка.
- [ ] Плавающие кнопки мессенджеров (Telegram / Viber / WhatsApp).
- [ ] Аналитика и цели.

### Этап 5. Запуск
- [ ] Контент от заказчика: реквизиты, контакты, логотип, цена монтажа.
- [ ] `grep` по `// TODO: заглушка` — ни одной не должно остаться.
- [ ] Фото работ, если появились → включить `FEATURES.showPortfolio`.
- [ ] Домен, SSL, деплой.
- [ ] Прогон Lighthouse, кроссбраузерная проверка, проверка форм.

### Этап 6. Админка
Пишем свою на Supabase. База, авторизация и хранилище картинок уже стоят с этапа 0 —
здесь остаётся только интерфейс.

- [ ] Роут `/admin`, закрыт Supabase Auth (email + пароль, **один пользователь**,
      регистрацию отключить). Страницу исключить из пререндера и `sitemap.xml`,
      закрыть в `robots.txt`.
- [ ] Товары: цена, описание, tier, featured, порядок, публикация/скрытие.
- [ ] Портфолио: загрузка фото в Storage, переключатель `showPortfolio`.
- [ ] Отзывы: добавление и модерация.
- [ ] Тексты готовых решений и цена монтажа.
- [ ] Заявки: список с датой, статусом и ответами квиза — заодно заменит блокнот.
- [ ] Кнопка «Опубликовать изменения» → build hook, с индикатором статуса сборки.

**Что позволило отложить админку в конец:** данные с самого начала лежат в Supabase,
а компоненты читают их только через `lib/queries.ts`. Админка — просто ещё один писатель
в ту же базу, переписывать приложение под неё не придётся.

---

## 11. Решения заказчика и что осталось открытым

### Согласовано (2026-09-03)

| Вопрос | Решение |
|---|---|
| Каталог | **44 модели по квотам** (20 сплит + по 8 мульти-сплит, мобильных, полупромышленных), отбор на нашей стороне. Фото и характеристики берём у поставщика. Критерии — §5. |
| Хранилище | **Supabase** — товары, отзывы, портфолио, заявки, фото. Схема и RLS — §5.2. |
| Наценка | **Нет, цена оборудования как у поставщика.** Коэффициент в конфиге на будущее. |
| Цены решений | Считаем от цен поставщика автоматически. |
| Фото работ, отзывы | **Нет.** На старте — заглушки, блок скрыт флагом (см. ниже). |
| Хостинг и домен | Отложено, вернёмся перед деплоем. |
| Обновление каталога | **Своя админка на Supabase в конце разработки** — этап 6. |

### Открыто — сейчас заглушки, поправим позже

| № | Что нужно | Где стоит заглушка | Насколько блокирует |
|---|---|---|---|
| 1 | **Цена монтажа** (одна цифра или таблица по мощности) | `config/pricing.ts`, `INSTALL_PRICE` | Единственная реально недостающая цифра. Без неё не показать «под ключ» — пока пишем «+ монтаж от N р.» |
| 2 | Название, слоган, логотип в векторе | `config/site.ts` + текстовый лого «КЛИМАТ ЛАЙН» | Не блокирует, меняется в одном месте |
| 3 | Реквизиты: ИП или самозанятый, УНП, адрес | `config/site.ts`, подвал и `/privacy` | Не блокирует вёрстку, но **обязательны до публикации** |
| 4 | Телефоны, мессенджеры, часы работы, зона выезда | `config/site.ts` | Не блокирует, но без них сайт не запустить |
| 5 | Согласование выгрузки с поставщиком | — | Стоит подтвердить письменно; заодно спросить про YML/CSV — надёжнее API |

**Все заглушки собраны в `config/site.ts` и `config/pricing.ts`** — один файл на текст,
один на цифры. Замена реальными данными не должна требовать правок в компонентах.
Помечаем каждую комментарием `// TODO: заглушка`, перед запуском проверяем `grep`-ом,
что ни одна не уехала в продакшен.

### Про фото работ

Их нет — и это самый дорогой пробел в контенте. Для частного мастера чужие стоковые
фото монтажа опаснее, чем отсутствие фото: клиент узнаёт сток и теряет доверие.
Поэтому: **блок «Работы и отзывы» верстаем, но прячем за флагом**
`FEATURES.showPortfolio = false`, пока не появятся реальные снимки.
Заказчику стоит просто отснять 5–10 своих установок на телефон — по отдаче
это самая ценная единица контента на сайте, дороже любого текста.

---

## 12. Бэклог (не в первый релиз)

- Калькулятор стоимости монтажа (длина трассы, этаж, штробление).
- Расширение каталога сверх 60 позиций — **только если пойдёт трафик** и мастер готов
  следить за ценами. Скрипт импорта это потянет, вопрос в поддержке, а не в коде.
- Сравнение моделей — на 40 позициях не нужно.
- Блог/статьи под SEO («как выбрать кондиционер по площади», «чистка или мойка»).
- Личный кабинет — **не нужен**, это не магазин.
- Онлайн-запись на сервис с выбором даты.
- Форма «Оставить отзыв» с модерацией.
- Тёмная тема — вряд ли нужна, решаем позже.
- Многоязычность — не требуется.

---

## 13. Журнал изменений плана

| Дата | Что изменилось |
|---|---|
| 2026-09-03 | Первая версия. Проверен API поставщика, снята дизайн-система с макета. |
| 2026-09-03 | Стек сменён с Next.js на **Vite + React Router + vite-react-ssg**. Переписаны §2, §6, §8, §9, этап 0. |
| 2026-09-03 | Каталог сокращён с ~800 до **30-60 курируемых моделей**, добавлены «готовые решения по площади». Цены — «от X р. под ключ». Переписаны §4, §5, §7, §9, этап 3, §11, §12. |
| 2026-09-03 | Главная сжата с **12 секций до 7**. Квиз развёрнут в полноценный §5.1 и поднят на второе место на странице. Добавлена страница `/quiz`, этап 3.1. |
| 2026-09-03 | Заказчик ответил по §11: топ-50 на наше усмотрение, наценки нет, фото работ нет, хостинг позже, админка в конце. Добавлены критерии отбора моделей (§5), таблица заглушек (§11), **этап 6 — админка**. |
| 2026-09-03 | Хранилище — **Supabase** вместо статического JSON. Добавлен §5.2 (схема, RLS, ключи, пересборка). Квоты импорта: 20 + 8×3 = 44. Переписаны §2, §6, §7, §8, этапы 0, 3, 6. |
| 2026-09-03 | Стилизация — **SCSS + CSS Modules** вместо Tailwind. Добавлен раздел «Организация стилей» в §3. |
| 2026-09-03 | Переработана карта сайта: индекс `/solutions`, каталог разведён на `/catalog/[category]` и `/product/[slug]`, добавлены `/price`, `/articles`, `/404`. Портфолио и отзывы — под флаг. Свои транслитерированные слаги. Меню сокращено до 5 пунктов. |
