export type HeroSlide = {
  avif: string;
  webp: string;
  /** Короткое описание для скринридера. */
  alt: string;
};

/**
 * Фоновая галерея hero — сменяется каждые 5 секунд.
 *
 * Пока пуста: подходящих фотографий нет. Стоковые сервисы автоматически
 * не забираются (Unsplash Source закрыт, прямые ссылки требуют валидных id),
 * а каталожные снимки поставщика — товар на белом фоне, для фона не годятся.
 *
 * КАК ДОБАВИТЬ:
 * 1. Положить исходники в `app/assets`, например `hero-bg-1.jpg`.
 * 2. Дописать их в JOBS в `scripts/optimize-images.mjs`.
 * 3. `npm run images` — получатся avif и webp в четырёх ширинах.
 * 4. Импортировать сюда и добавить в массив ниже.
 *
 * Пока массив пуст, галерея не рендерится вовсе и остаётся текущий градиент.
 * Одного слайда тоже достаточно: смена не запускается, показывается статично.
 */
export const heroSlides: HeroSlide[] = [];

/** Интервал смены. Меньше 5 секунд человек не успевает прочитать заголовок. */
export const SLIDE_INTERVAL = 5000;
