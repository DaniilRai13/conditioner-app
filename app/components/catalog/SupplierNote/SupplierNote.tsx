import { site } from "~/config/site";
import styles from "./SupplierNote.module.scss";

/**
 * Широта ассортимента текстом: на витрине 50 моделей из четырёх тысяч,
 * и без этой строки каталог выглядит скудным.
 *
 * Прилипает к низу окна, пока листается сетка. Мысль «нужной модели нет»
 * приходит во время просмотра, а не после того, как человек домотал
 * до конца страницы — к тому моменту он обычно уже ушёл.
 */
export function SupplierNote() {
  return (
    <p className={styles.note}>
      Работаю со всем каталогом поставщика — больше 4000 моделей. Нужной нет на
      сайте? <a href={site.phoneHref}>Позвоните</a>, привезу под заказ.
    </p>
  );
}
